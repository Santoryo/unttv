{
  "private": true,
  "scripts": {
    "prepare": "nitropack prepare",
    "dev": "nitropack dev",
    "build": "nitropack build --preset cloudflare_module",
    "preview": "wrangler dev .output/server/index.mjs --site .output/public --local",
    "deploy": "wrangler deploy --site .output/public"
  },
  "dependencies": {
    "@twurple/api": "^7.0.8",
    "@twurple/auth": "^7.0.8",
    "@upstash/ratelimit": "^1.0.0",
    "@upstash/redis": "^1.28.2",
    "dotenv": "^16.4.1",
    "nitropack": "latest"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
